syntax = "proto3";

package com.clickhouse.kafka.connect.proto.test;

message ArrayWithMapInTuple {
  int32 id = 1;
  repeated OuterItem items = 2;

  message OuterItem {
    int32 item_id = 1;
    map<string, string> attributes = 2;
  }
}
