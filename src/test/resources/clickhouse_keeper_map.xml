<clickhouse>
    <!--
        Configuration for the dockerized ClickHouse used by standalone tests.

        Enables the KeeperMap table engine so EO tests
        (KeeperStateProvider) can create and query connect_state.
        A single-node embedded clickhouse-keeper backs it; the server then
        points its <zookeeper> entry at that local instance.
    -->

    <keeper_map_path_prefix>/clickhouse/kafka_connect</keeper_map_path_prefix>

    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>warning</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <zookeeper>
        <node>
            <host>localhost</host>
            <port>9181</port>
        </node>
    </zookeeper>
</clickhouse>
